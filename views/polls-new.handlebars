<div class="col s12">
  <div class="section">
    <h2 class="center-align">Welcome to <span class="logo-text text-cyan" style="font-size:1.3em;color:#00C8DC;">OmniVox</span></h2>
    <div class="divider">


    </div>
    <h5 class="center-align">
      Super-simple, super-fast polling for groups.
    </h5>
    <p class="center-align">
      Enter in the unique path of your poll to either join or create a new one.
    </p>
  </div>

  <div class="col s10 offset-s1">
    <form action='polls-new' id='poll-form'>
      <fieldset>

        <div class="row">

            <div class="input-field inline col s12">
              <i class="large material-icons prefix">label_outline</i>

              <input id="path" name="path" type="text" placeholder="Enter Your Vox Code or Enter a New Vox Custom Code" required="">
              <label for="path">Path or Code</label>
            </div>

          </div>
          <div>
            <button id="submit" class="btn btn-info">Go!</button>
          </div>
        </div>

    </fieldset>
  </form>
</div>




{{!-- <div id="polls">
{{#each polls}}
<div class="poll-item">
<div class="plan-name"><a href="/polls/{{this._id}}">{{this.title}}</a></div>
<div class="edit-poll pull-right" data-id="{{this._id}}">
<button type="button" action="/polls/edit/{{this._id}}" class="btn btn-default">Edit <span class="glyphicon glyphicon-edit"></span> </button>
</div>
<div class="remove-poll pull-right" data-id="{{this._id}}">
<button type="button" class="btn btn-default">Remove <span class="glyphicon glyphicon-minus"></span> </button>
</div>
<div class="text"><p><span class="label label-default">{{this.category}}</span></p></div>

</div>
{{/each}}
</div> --}}


</div>
